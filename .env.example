# Copy this file to .env and fill in the value for your provider.
# Required for the default OpenAI-backed setup used by the benchmark harness.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: override the default seed dataset when not using a scenario harness.
# TOOL_DATA_SET=default
